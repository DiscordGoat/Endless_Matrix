# Project Instructions

- After any gameplay, UI, asset, routing, or build-output change, run `npm run build:pages` before finishing so `docs/` stays synced for GitHub Pages.
- GitHub Pages serves from `docs/`; do not stop after `npm run build` when the change should be visible on the deployed site.
